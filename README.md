# RUSH: shell written completely in Rust

### Dependencies:
    - Rust language

### Setup:
    - clone repo
    - ```cd``` into rush directory
    - run ```cargo build```
    - to test run ```cargo run```

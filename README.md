# RUSH: shell written completely in Rust

### Dependencies:
    - Rust language

### Setup:
    - clone repo
    - then do the following:
    ```
    $ cd path/to/repository/
    $ cargo build
    $ cargo run
    ```
